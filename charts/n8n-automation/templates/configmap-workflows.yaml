apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "n8n-workflows.fullname" . }}-n8n-workflows
  labels:
    {{- include "n8n-workflows.labels" . | nindent 4 }}
data:
  {{- range $path, $_ := .Files.Glob "n8n-workflows/*" }}
  {{ base $path }}: |-
{{ $.Files.Get $path | indent 4 }}
  {{- end }}