apiVersion: v1
kind: ConfigMap
metadata:
  name: shared-configmap-rag
  labels:
    {{- include "nh-intel.labels" . | nindent 4 }}
data:
  POSTGRES_HOST: {{ .Values.rag.postgresHost }}
  POSTGRES_USERNAME: {{ .Values.global.postgresql.auth.username }}
  NH_RAG_API_URL: {{ .Values.rag.ragApiUrl }}
  {{- with (index .Values "nh-mcp-server") }}
  {{- if and .enabled .config .config.url }}
  NH_MCP_SERVER_URL: {{ .config.url | quote }}
  {{- end }}
  {{- end }}
  {{- with (index .Values "nh-mcp-server-v2") }}
  {{- if and .enabled .config .config.url }}
  NH_MCP_SERVER_V2_URL: {{ .config.url | quote }}
  {{- end }}
  {{- end }}
  NODE_ENV: {{ .Values.global.nodeEnv }}
  JUICEFS_S3_ENDPOINT: {{ .Values.global.juiceFsUrl | quote }}
