camel-k:
  enabled: true

  # These values will be passed directly to the upstream chart
  operator:
    namespace: nh-addons
    logLevel: info

  # Platform configuration
  platform:
    profile: Kubernetes

  monitoring:
    enabled: false
